Very light boilerplate project to run Cucumber scenarios on Chrome headless using WebdriverIO

### Requirements
Chrome version >= 58 installed on the machine (Linux or MacOS only)

### Scripts
Run the test with `yarn e2e` or `npm run e2e`



